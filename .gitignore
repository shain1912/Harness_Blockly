node_modules/
.env
.env.*
dist/
test-results/
playwright-report/
*.deb
.DS_Store
*.log

# Scratch reference screenshots pasted during development
image.png
image copy*.png

# Verification-loop scratch output (driver screenshots + JSON)
tmp/

# Vendored offline runtime assets (reproduced from node_modules by scripts/vendor-assets.cjs)
public/vendor/

# electron-builder output
release/

# Bundled portable Python (regenerated by scripts/bundle-python.py)
python-embed/

# Per-machine AI key config saved by the in-app Settings (never commit secrets)
blockpy-config.json
